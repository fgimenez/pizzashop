version: 2
jobs:
    install_dependencies:
        working_directory: /tmp/tacoshop
        docker:
            - image: ubuntu
        steps:
            - checkout
            - run: mkdir -p bin
            - run:
                name: download-architect
                command: |
                    apt update && apt install -y --no-install-recommends ca-certificates wget curl git
                    wget $(curl -sS https://api.github.com/repos/giantswarm/architect/releases/latest | grep browser_download_url | head -n 1 | cut -d '"' -f 4) -O bin/architect
                    chmod +x ~/tacoshop/bin/architect
                    /tmp/tacoshop/bin/architect version
            - persist_to_workspace:
                root: bin
                paths:
                    - architect

    build:
        working_directory: /tmp/tacoshop
        docker:
            - image: ubuntu
        steps:
            - attach_workspace:
                at: /tmp/tacoshop/bin
            - run:
                command: |
                    apt update && apt install -y --no-install-recommends ca-certificates git
                    /tmp/tacoshop/bin/architect --organisation=fgimenez build
    deploy:
        working_directory: /tmp/tacoshop
        docker:
            - image: ubuntu
        steps:
            - attach_workspace:
                at: /tmp/tacoshop/bin
            - run:
                command: |
                    apt update && apt install -y --no-install-recommends ca-certificates git
                    /tmp/tacoshop/bin/architect --organisation=fgimenez deploy

workflows:
    version: 2
    build_and_deploy:
        jobs:
            - install_dependencies
            - build:
                    requires:
                        - install_dependencies
            - deploy:
                    requires:
                        - build
                    filters:
                        branches:
                            only: master
